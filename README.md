# 📚 مجالس الوراقين - Majalis Al-Warraqeen

نادي النخبة للقراءة ومناقشة أمهات الكتب

## 🎯 نظرة عامة

**مجالس الوراقين** هو منصة ويب حديثة لإدارة نادي القراءة، حيث يمكن للأعضاء:
- 📖 استكشاف مكتبة الكتب
- 💬 مناقشة الكتب والتعليق عليها
- 📝 تقديم طلبات الانضمام
- ⭐ تقييم وتقديم آرائهم

## 🛠️ التقنيات المستخدمة

- **Frontend:** HTML, CSS (SCSS), JavaScript (ES6+)
- **Framework:** Bootstrap 5
- **Build Tool:** Webpack
- **Backend:** Supabase (Firebase Alternative)
- **Database:** PostgreSQL (عبر Supabase)
- **UI Library:** SweetAlert2

## 📋 المتطلبات

- Node.js >= 14
- npm >= 6

## 🚀 البدء السريع

### 1. استنساخ المستودع
```bash
git clone https://github.com/muhammet-g/majalis.git
cd mejalis
```

### 2. تثبيت المكتبات
```bash
npm install
```

### 3. إعداد متغيرات البيئة
أنشئ ملف `.env` في الجذر:
```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
```

### 4. تشغيل الموقع
```bash
npm start
```

سيفتح تلقائياً على: `http://localhost:8080`

## 📂 هيكل المشروع

```
mejalis/
├── src/
│   ├── index.html          # الملف الرئيسي
│   ├── js/
│   │   ├── app.js          # نقطة البداية
│   │   ├── auth.js         # نظام المصادقة
│   │   ├── router.js       # موجه التطبيق
│   │   ├── services.js     # خدمات Supabase
│   │   ├── supabaseClient.js # عميل Supabase
│   │   └── pages/
│   │       ├── home.js
│   │       ├── library.js
│   │       ├── bookDetails.js
│   │       └── admin.js
│   └── scss/
│       └── main.scss       # الأنماط الرئيسية
├── webpack.config.js       # إعدادات Webpack
├── package.json
└── README.md
```

## 🔐 نظام المصادقة

المستخدمون لديهم ثلاث حالات:
- **زائر**: يمكنه عرض الصفحة الرئيسية فقط
- **قيد الانتظار**: تم تقديم طلب انضمام، في انتظار الموافقة
- **نشط**: عضو معتمد يمكنه الوصول للمكتبة الكاملة

## 📱 الميزات الرئيسية

### 🏠 الصفحة الرئيسية
- معلومات عن النادي
- نموذج تقديم الانضمام
- نموذج تسجيل دخول

### 📚 المكتبة
- عرض جميع الكتب (للأعضاء النشطين فقط)
- بطاقات الكتب مع الصور

### 📖 تفاصيل الكتاب
- معلومات كاملة عن الكتاب
- التقييمات والتعليقات
- إمكانية إضافة تقييمات (للأعضاء)

### 🛡️ لوحة التحكم
- إدارة الأعضاء (للمشرفين فقط)
- قبول/رفض طلبات الانضمام
- عرض حالة الأعضاء

## 🗄️ قواعد البيانات

### جدول Members
```sql
id (UUID) - المعرف الفريد
email (TEXT) - البريد الإلكتروني
name (TEXT) - الاسم
phone (TEXT) - رقم الهاتف
reason (TEXT) - سبب الانضمام
status (ENUM) - الحالة: pending, active
role (ENUM) - الدور: member, admin
created_at (TIMESTAMP) - تاريخ الإنشاء
```

### جدول Books
```sql
id (UUID) - المعرف الفريد
title (TEXT) - العنوان
coverImage (TEXT) - رابط الغلاف
status (ENUM) - الحالة: read, current, upcoming
created_at (TIMESTAMP) - تاريخ الإضافة
```

### جدول Reviews
```sql
id (UUID) - المعرف الفريد
bookId (UUID) - معرف الكتاب
memberId (UUID) - معرف العضو
rating (INT) - التقييم 1-5
reviewText (TEXT) - نص التعليق
created_at (TIMESTAMP) - تاريخ التعليق
```

## 📖 الأوامر المهمة

### التطوير
```bash
npm start          # تشغيل خادم التطوير
npm run build      # بناء للإنتاج
```

### الأنماط
- الألوان:
  - **أزرق داكن:** `#041E3B`
  - **ذهبي:** `#CD9B14`
  - **فضي:** `#E5E5E5`

## 🐛 استكشاف الأخطاء

### المشكلة: الموقع بطيء
- تحقق من الإنترنت
- امسح cache المتصفح
- أعد تشغيل خادم التطوير

### المشكلة: خطأ في Supabase
- تحقق من `.env` file
- تأكد من إنشاء الجداول
- تأكد من توفر الإنترنت

## 📧 التواصل

للأسئلة والاقتراحات: [أضف بريدك الإلكتروني]

## 📄 الترخيص

هذا المشروع مفتوح المصدر تحت ترخيص MIT

---

تم إنشاؤه بـ ❤️ لعشاق القراءة

**تاريخ البدء:** January 2026
