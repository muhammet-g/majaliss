# 🔐 دليل نظام المصادقة والتسجيل

## 📋 نظرة عامة

تم إضافة نظام مصادقة كامل يتعامل مع:
- ✅ تسجيل الدخول عبر البريد الإلكتروني
- ✅ التحقق من حالة المستخدم (نشط/قيد الانتظار)
- ✅ حفظ بيانات المستخدم في localStorage
- ✅ عرض منطقة الأعضاء عند تسجيل الدخول
- ✅ تسجيل الخروج الآمن

---

## 🏗️ البنية الهندسية

### الملفات الجديدة/المحدثة

| الملف | الوصف |
|------|-------|
| `src/js/auth.js` | ملف المصادقة الرئيسي (جديد) |
| `src/js/app.js` | تم تحديثه لدعم نظام المصادقة |
| `src/index.html` | تم إضافة قسم الدخول في الـ Navbar |
| `src/scss/main.scss` | تم إضافة أنماط لزر الدخول |

---

## 🔧 الدوال الرئيسية في auth.js

### 1. `loginUser(email)`

**الوصف:** تسجيل دخول المستخدم بناءً على البريد الإلكتروني

**المعاملات:**
- `email` (string) - البريد الإلكتروني

**القيمة المرجعة:**
- Object - بيانات المستخدم عند النجاح
- null - عند الفشل

**السلوك:**

```javascript
// مثال الاستخدام
const user = await loginUser('user@example.com');
```

| الحالة | الرد |
|--------|------|
| المستخدم نشط (`status === 'active'`) | ✅ حفظ في localStorage + رسالة ترحيب |
| المستخدم قيد الانتظار (`status === 'pending'`) | ⚠️ رسالة تحذير |
| المستخدم غير موجود | ❌ رسالة خطأ: "بريد غير مسجل" |
| خطأ في قاعدة البيانات | ❌ رسالة خطأ عامة |

---

### 2. `logoutUser()`

**الوصف:** تسجيل خروج المستخدم وحذف بياناته من localStorage

**المعاملات:** لا توجد

**السلوك:**
- ✅ طلب تأكيد من المستخدم
- ✅ حذف `loggedUser` من localStorage
- ✅ إعادة التحميل وإعادة التوجيه للصفحة الرئيسية

```javascript
// مثال الاستخدام
logoutUser();
```

---

### 3. `getLoggedUser()`

**الوصف:** الحصول على بيانات المستخدم المسجل من localStorage

**المعاملات:** لا توجد

**القيمة المرجعة:**
- Object - بيانات المستخدم
- null - إذا لم يكن هناك مستخدم مسجل

```javascript
// مثال الاستخدام
const user = getLoggedUser();
if (user) {
    console.log('مرحباً', user.name);
}
```

---

### 4. `isLoggedIn()`

**الوصف:** التحقق من حالة تسجيل الدخول

**المعاملات:** لا توجد

**القيمة المرجعة:**
- `true` - إذا كان المستخدم مسجلاً
- `false` - إذا لم يكن مسجلاً

```javascript
// مثال الاستخدام
if (isLoggedIn()) {
    console.log('مستخدم مسجل');
} else {
    console.log('مستخدم غير مسجل');
}
```

---

### 5. `updateLoggedUser(updatedUser)`

**الوصف:** تحديث بيانات المستخدم المسجل في localStorage

**المعاملات:**
- `updatedUser` (Object) - البيانات المحدثة

**السلوك:**
- ✅ حفظ البيانات الجديدة في localStorage

```javascript
// مثال الاستخدام
const updatedUser = {
    ...currentUser,
    role: 'admin'
};
updateLoggedUser(updatedUser);
```

---

### 6. `showLoginForm()`

**الوصف:** عرض نموذج تسجيل الدخول (SweetAlert2)

**المعاملات:** لا توجد

**القيمة المرجعة:**
- Object - بيانات المستخدم عند النجاح
- null - عند الإلغاء أو الفشل

**السلوك:**
- ✅ عرض مربع إدخال البريد الإلكتروني
- ✅ التحقق من صحة البريد
- ✅ استدعاء `loginUser()` تلقائياً

```javascript
// مثال الاستخدام
const user = await showLoginForm();
```

---

## 💾 هيكل البيانات المحفوظة

عند تسجيل الدخول بنجاح، يتم حفظ كائن المستخدم في localStorage:

```javascript
localStorage['loggedUser'] = JSON.stringify({
    id: "uuid-123",
    email: "user@example.com",
    name: "محمد أحمد",
    phone: "9665551234",
    reason: "أحب القراءة",
    status: "active",
    role: "admin",
    created_at: "2026-01-29T..."
});
```

---

## 🎨 واجهة المستخدم

### قبل تسجيل الدخول

```
┌─────────────────────────────────────┐
│ مجالس الوراقين  |  الرئيسية | المكتبة  |
│                         [دخول]     │
└─────────────────────────────────────┘
```

### بعد تسجيل الدخول

```
┌──────────────────────────────────────┐
│ مجالس الوراقين  |  الرئيسية | المكتبة  |
│                 محمد [خروج]        │
└──────────────────────────────────────┘
```

---

## 🔄 تدفق تسجيل الدخول

```
┌─────────────────────────────────────────┐
│ المستخدم ينقر على زر "دخول"           │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│ عرض نموذج البريد الإلكتروني (SweetAlert2)│
└────────────────┬────────────────────────┘
                 │
        ┌────────┴────────┐
        ▼                 ▼
    [البريد]          [إلغاء]
        │                 │
        ▼                 ▼
┌──────────────────┐  ❌ إلغاء العملية
│ تحقق من صحة البريد│
└────────┬─────────┘
         │
    ┌────┴─────┐
    ▼          ▼
 ✅ صحيح   ❌ خطأ
    │          │
    ▼          ▼
 البحث في   رسالة خطأ
 قاعدة البيانات
    │
    ├─── ✅ وجد (نشط) ────► حفظ في localStorage ─► ✅ نجاح
    │
    ├─── ⏳ وجد (قيد الانتظار) ────► ⚠️ رسالة تحذير
    │
    └─── ❌ غير موجود ────► ❌ رسالة خطأ
```

---

## 🧪 اختبار نظام المصادقة

### 1. اختبار تسجيل الدخول بنجاح

**الخطوات:**
1. تأكد من وجود مستخدم نشط في جدول members:
   ```sql
   SELECT * FROM members WHERE status = 'active' LIMIT 1;
   ```

2. افتح الموقع في المتصفح
3. انقر على زر "دخول"
4. أدخل البريد الإلكتروني للمستخدم النشط
5. يجب أن يظهر:
   - ✅ رسالة ترحيب
   - ✅ الاسم يظهر في Navbar
   - ✅ زر "خروج" بدلاً من "دخول"

**التحقق من localStorage:**
```javascript
// في Console (F12)
console.log(JSON.parse(localStorage.getItem('loggedUser')));
```

---

### 2. اختبار بريد قيد الانتظار

**الخطوات:**
1. أضف مستخدم قيد الانتظار:
   ```sql
   INSERT INTO members (email, name, status, role) 
   VALUES ('pending@example.com', 'محمد', 'pending', 'member');
   ```

2. حاول تسجيل الدخول بـ `pending@example.com`
3. يجب أن يظهر:
   - ⚠️ رسالة تحذير: "طلب قيد المراجعة"
   - ❌ عدم حفظ البيانات في localStorage

---

### 3. اختبار بريد غير مسجل

**الخطوات:**
1. حاول تسجيل الدخول بـ بريد غير موجود
2. يجب أن يظهر:
   - ❌ رسالة خطأ: "بريد غير مسجل"

---

### 4. اختبار تسجيل الخروج

**الخطوات:**
1. سجل الدخول بنجاح
2. انقر على زر "خروج"
3. يجب أن:
   - ✅ يظهر تأكيد الخروج
   - ✅ يتم حذف البيانات من localStorage
   - ✅ يظهر زر "دخول" مرة أخرى

---

## 🔍 تتبع الأخطاء

### فتح Console

اضغط **F12** وافتح تبويب **Console** لرؤية الرسائل التالية:

```javascript
// تسجيل دخول ناجح
✅ تم تسجيل الدخول بنجاح: محمد

// تسجيل خروج ناجح
✅ تم تسجيل الخروج

// مستخدم مسجل عند فتح الصفحة
✅ User logged in: محمد

// بدون مستخدم مسجل
No user logged in
```

---

## 📱 الرسائل المعروضة

### 1. رسالة الترحيب (عند النجاح)

```
Icon: ✅
Title: مرحباً!
Message: أهلاً وسهلاً محمد
```

### 2. رسالة الانتظار (قيد الانتظار)

```
Icon: ⚠️
Title: طلب قيد المراجعة
Message: طلب الانضمام قيد المراجعة من قبل الإدارة
```

### 3. رسالة البريد غير المسجل

```
Icon: ❌
Title: بريد غير مسجل
Message: هذا البريد الإلكتروني غير مسجل في النظام
```

### 4. رسالة الخطأ العامة

```
Icon: ❌
Title: خطأ في تسجيل الدخول
Message: حدث خطأ أثناء معالجة طلبك. يرجى المحاولة لاحقاً
```

---

## ⚙️ التكامل مع باقي التطبيق

### استخدام المستخدم الحالي

في أي ملف، يمكنك الوصول إلى المستخدم الحالي:

```javascript
import { getLoggedUser, isLoggedIn } from './auth.js';

if (isLoggedIn()) {
    const currentUser = getLoggedUser();
    console.log('اسم المستخدم:', currentUser.name);
    console.log('الدور:', currentUser.role);
}
```

### تحديد الصلاحيات

```javascript
if (isLoggedIn()) {
    const user = getLoggedUser();
    
    if (user.role === 'admin') {
        // عرض لوحة التحكم
    } else if (user.status === 'active') {
        // عرض محتوى الأعضاء
    }
}
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة: زر الدخول لا يظهر

**الحل:**
- تأكد من أن `src/index.html` يحتوي على `<div id="auth-section">`
- تحقق من أن `app.js` يستدعي `updateAuthUI()` بعد `initializeAppData()`

---

### المشكلة: البيانات لا تُحفظ في localStorage

**الحل:**
- تأكد من أن المتصفح يسمح بـ localStorage
- جرب حذف localStorage والبدء من جديد:
```javascript
localStorage.clear();
location.reload();
```

---

### المشكلة: الاسم لا يظهر في Navbar بعد الدخول

**الحل:**
- أعد تحميل الصفحة (F5)
- تحقق من أن المستخدم لديه حقل `name` في قاعدة البيانات

---

## ✅ قائمة التحقق

- [ ] اختبرت تسجيل الدخول بنجاح
- [ ] اختبرت بريد قيد الانتظار (يجب أن يُحظر)
- [ ] اختبرت بريد غير موجود (يجب أن يظهر خطأ)
- [ ] اختبرت تسجيل الخروج
- [ ] تحققت من localStorage يحتوي على البيانات
- [ ] تحققت من أن الاسم يظهر في Navbar
- [ ] اختبرت إعادة تحميل الصفحة (يجب أن يبقى المستخدم مسجلاً)

---

## 📚 موارد إضافية

- [SweetAlert2 Documentation](https://sweetalert2.github.io/)
- [localStorage API](https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage)
- [JSON stringify/parse](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON)
